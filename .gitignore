# -----------------------------------------------------------------------------
# .gitignore
#
# This file specifies intentionally untracked files that Git should ignore.
# Files already tracked by Git are not affected by this file.
#
# Each line in this file represents a pattern.
# - Blank lines or lines starting with # are comments.
# - Trailing spaces are ignored unless quoted.
# - An optional prefix "!" which negates the pattern.
# - A slash "/" at the beginning means the pattern matches only at the
#   repository root.
# - A slash "/" at the end means the pattern matches directories only.
# -----------------------------------------------------------------------------

# Logs
# Ignore various log files generated during development or by package managers.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories and build outputs
# Ignore the 'node_modules' directory which contains installed packages,
# and common build output directories like 'dist' and 'dist-ssr'.
node_modules
dist
dist-ssr

# Local configuration files
# Ignore files that contain local environment configurations or sensitive data.
# The '*.local' pattern is often used for environment-specific settings.
*.local

# Editor and IDE specific directories and files
# Ignore files and directories generated by various Integrated Development Environments (IDEs)
# to prevent them from cluttering the repository.

# Visual Studio Code specific files
# Ignore the .vscode directory, but explicitly include extensions.json
# to ensure recommended extensions are tracked.
.vscode/*
!.vscode/extensions.json

# IntelliJ IDEA / WebStorm specific files
# Ignore the .idea directory which contains project settings for JetBrains IDEs.
.idea

# macOS specific files
# Ignore .DS_Store files, which are generated by macOS to store custom attributes of a folder.
.DS_Store

# Visual Studio / Windows specific files
# Ignore various files generated by Visual Studio or related tools.
*.suo
*.ntvs*
*.njsproj
*.sln

# Swap files generated by Vim/Neovim
# Ignore temporary swap files created by Vim-like editors.
*.sw?
